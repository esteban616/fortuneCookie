
// eslint-disable-next-line react/prop-types
const Author = ({phraseRandom}) => {
  return (
    // eslint-disable-next-line react/prop-types
    <p className="authorCard">fuente: {phraseRandom.author}</p>
  )
}

export default Author